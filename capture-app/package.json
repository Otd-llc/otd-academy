{
  "name": "otd-capture",
  "productName": "OTD Capture",
  "version": "0.1.0",
  "private": true,
  "description": "One Thousand Drones Academy — desktop screen-capture companion. The lesson '+' launches it via the otd-capture:// deep link; it shows what to capture, frames a marching-ants box over any app (content-protected, global spacebar), and on approve uploads straight into the lesson slot. Standalone fallback saves to disk.",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^33.2.0"
  }
}
