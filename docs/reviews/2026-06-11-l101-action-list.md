# L1.01 — actionable list (from the 2026-06-11 focus panel)

Working checklist derived from [the focus-panel review](2026-06-11-l101-focus-panel.md), folding in the three decisions from 2026-06-11:
- **Physical-build captures wait** until we order + build the board ourselves — and the empty slots should be **hidden from students** but stay visible to admins (so we can shoot them later).
- **Stripboard validation: remove** (reintroduce as its own section/lesson if ever needed).
- **Delete the two test screenshots** (LAYOUT [03], [17]) — they were just capture tests.

Legend: 🟦 student · 🟨 teacher · 🟧 engineer · 🟩 admin · ⏳ deferred (needs physical build) · 💲 prod write · 🔧 code change.

---

## ✅ Execution log — 2026-06-11

**Batch 1 (decisions):** A1 (hide empty placeholders — code + tests), A2 (unwire stripboard from L1.01), A3 (delete the two test screenshots + R2 objects), D1 (BOM checklist). All done + verified.

**Batch 2 ("do all" — content patch, applied transactionally to prod; all 8 cards re-verified to safeParse so none render blank):**
- **Captures (placeholders staked out with captureHints + aspect, admin-only via A1):** B4/B5/B6 (LAYOUT [54]/[32]/[37] hints), LAYOUT [17] floor-plan hint, B7 (SCHEMATIC ERC-dialog), B8 (DRC dialog + Gerber viewer), B9 (keep-out rule-area screenshot), B10 (first-KiCad-gesture clip), B11 (Insert-key clip), B13/B14 (ASSEMBLY drag-solder + U1/J1 hints), B15 (bridge-vs-clean macro), B16 (BRINGUP rail-probe hint), B17 (flash-dance clip).
- **Content:** C1 (header "first column with me" worked example), C2 (how to find unused pins via ERC), C4 (RT9080 dropout — 0.31 V typ / 0.53 V max), C6 (keep-out Rule Area menu recipe), C7 (how to free-place stitching vias), C8 (named the "checker is blind to intent" pattern), C9 (reframed DRC/Gerber self-check), C10 (fuller drag-solder worked beat).
- **Assessment:** E1 (SCHEMATIC Q8 — header both-ends/mislabel trap), E2 (REQUIREMENTS + BOM giveaway distractors replaced with plausible-wrong), E3 (ORDERING ENIG-judgment question).
- **F1:** rail-voltage cheat-sheet **table** in BRINGUP (chose a table over a hand-authored SVG — same diagnostic logic, zero authoring risk).

**Deliberately NOT done (my call — say the word and I'll do any of them):**
- **D2 — BRINGUP gate evidence artifact:** *deferred.* Changing the final stage's `completionRef` off self-asserted `boardStatus` is a real behavioral change wired across the platform (board-status flow, `load-gate-context`, `StageGate`). Wants its own deliberate change, not a silent content patch.
- **D3 — extra vendor CTAs (PCBWay in LAYOUT, Newark in ASSEMBLY):** *skipped on taste.* A PCBWay *order* CTA mid-layout is premature (no Gerbers yet) and a 3rd Newark CTA risks reading pushy. The existing ORDERING CTAs are already the right, restrained model.
- **C3 (D+/D− prose/table order), C11 (continuity recap dedupe), F2 (refdes cast-list):** *skipped* — all minor, and not worth unicode-fragile surgery / clutter on the flagship card.
- **LAYOUT [03] cross-section:** left empty (admin-only). It's a *concept diagram*, not a KiCad screenshot — wants an authored `/guide-diagrams/*.svg`, so no captureHint added.
- **B12 (diff-pair router clip):** skipped — the static [32] shot + B10/B11 clips already cover the "magic".

---

## A. Decisions in flight (do these first) — ✅ DONE 2026-06-11

- [x] **A1 — Hide empty media placeholders from students** 🔧🟦🟩 ✅
  [GuideBlocks.tsx](src/components/guide/GuideBlocks.tsx): empty image/video now renders `null` for non-admins; admin branch (placeholder + `CaptureLauncher` "+") unchanged. Test-first: [GuideBlocks.test.tsx](src/components/__tests__/GuideBlocks.test.tsx) (5 tests, green); tsc clean. *Effect: every unshot slot — drag-solder clip, board renders, bench photos — is invisible to students but a one-click capture target for us.*
- [x] **A2 — Remove stripboard validation (unwired L1.01 only)** 💲🟩 ✅
  L1.01 BOM_SOURCING `completionRef`: `STRIPBOARD_VALIDATION` → `{kind:"none"}`; the empty "STRIPBOARD validation checklist" row deleted. Verified: 0 STRIPBOARD checklists remain. The enum/gate/`requiresStripboard` flag/templates are intact for the unbuilt l1-02→l1-05 (per the chosen scope). *If we ever want stripboard, it gets its own section + lesson.*
- [x] **A3 — Delete the two test screenshots** 💲🟩 ✅
  LAYOUT [03] + [17] `src` reset to `""`; both R2 objects (`guide-shots/h169vii0u5xt8291hbe89hl3.webp`, `…fiptw05noclqwkfydqxdvc4u.webp`) deleted. LAYOUT card re-validated (58 blocks). With A1, these empty slots are admin-only. *Their captions already read "(Reference render to come.)" — now accurate again, so no caption fix needed (supersedes C5 for these two).*

---

## B. Captures — by effort tier

### Tier A · CHEAP (KiCad/browser screenshots, no physical build) — do now
- [ ] **B1 — LAYOUT [10]** PCBWay GitHub `.zip` Download (⤓) button. Shot · 16:10 · *hint already authored.* 🟦🟨🟧🟩
- [ ] **B2 — LAYOUT [11]** Board Setup ▸ Constraints with the four minimums typed in. Shot · 16:10 · *hint authored.* 🟦🟨🟧🟩
- [ ] **B3 — LAYOUT [12]** Custom Rules panel showing PCBWay rule text loaded. Shot · 16:10 · *hint authored.* 🟦🟨🟧🟩
- [ ] **B4 — LAYOUT [54]** finished board top-view = the layout answer key. Shot · 16:10 · **add captureHint.** 🟦🟨🟧🟩
- [ ] **B5 — LAYOUT [32]** routed USB diff-pair over pour. Shot (or clip) · 16:10 · **add captureHint.** 🟦🟨🟧🟩
- [ ] **B6 — LAYOUT [37]** bottom ground pour filled + stitched, antenna notch. Shot · 16:10 · **add captureHint.** 🟦🟨🟧🟩
- [ ] **B7 — SCHEMATIC (new, near [90]/[92])** ERC dialog at 0 errors = the gate artifact. Shot · 16:10 · net-new block. 🟩🟧
- [ ] **B8 — DRC_GERBER (new, near [02]/[05])** DRC-at-0 dialog + a Gerber-viewer shot. Shot ×2 · 16:10 · net-new blocks. 🟩🟨🟧
- [ ] **B9 — LAYOUT (new, near [20]/[43])** the antenna keep-out rule area drawn under U1. Shot · 16:10 · net-new block. 🟦

### Tier B · MED (short screen-recordings) — do when convenient
- [ ] **B10 — SCHEMATIC (new, near [67])** the *first KiCad gesture*: open project, M to move, W to wire one pin-to-pin. Clip · 16:9. 🟦🟨 *(closes the biggest "zero outside help" gap)*
- [ ] **B11 — SCHEMATIC (new, near [55]/[83])** Insert-key auto-increment marching IO4→IO5→IO6. Clip · 16:9. 🟦🟨
- [ ] **B12 — LAYOUT [32] (optional upgrade)** diff-pair router mid-route (the "magic" moment). Clip · 16:9. 🟨

### Tier C · ⏳ DEFERRED — needs the physical build; hidden from students via A1
- [ ] **B13 — ASSEMBLY [10]** one real drag-solder pass. Clip · 16:9 · **add captureHint now.** 🟦🟨🟧 *(highest pedagogical value)*
- [ ] **B14 — ASSEMBLY [06]** U1+J1 placed before passives. Shot · 16:10 · **add captureHint now.** 🟦🟨🟩
- [ ] **B15 — ASSEMBLY (new, near [11]/[12])** macro: solder bridge vs clean joint. Shot · 1:1. 🟨
- [ ] **B16 — BRINGUP [10]** meter reading ~3.30 V at TP1/TP2. Shot · 16:10 · **add captureHint now** (lower priority — SVG [07] already covers it). 🟩🟧
- [ ] **B17 — BRINGUP (new, near [12])** the EN/BOOT flash dance. Clip · 16:9. 🟦🟨

*(All draft captureHints for B4–B17 are in the shot-list table of the review doc — copy them straight in.)*

---

## C. Content fixes (per stage)

### SCHEMATIC
- [ ] **C1** Move the Insert-key tip ([83]) up to sit with the header table ([55]); add a "do the first J2 column with me" worked example before the 44-pin march. 🟦🟨
- [ ] **C2** Tell the learner how to *find* the unused pins for no-connect flags ([85]) — "they show as unconnected in the ERC list," or a cropped J1 symbol with SBU/NC circled. 🟦
- [ ] **C3** Keep the D+/D− order identical between prose [47] and table [48]. 🟦
- [ ] **C4** Tighten the RT9080 dropout wording in [08]/deepDive: "up to ~0.53 V worst-case (≈0.31 V typical)." 🟧

### LAYOUT
- [ ] **C5** Strip the stale "(Reference render to come.)" from LAYOUT [03] and [17] captions (the shots exist — but per A3 we're clearing them, so reword to a forward-looking caption or drop the parenthetical). 🟩
- [ ] **C6** Give the antenna keep-out a 2-step menu recipe in place-it STEPS [43] step 2 (Place ▸ Rule Area → box under antenna → all copper layers + silk → keep out copper/zones). 🟦
- [ ] **C7** Add one line on how to free-place stitching vias ([36]/[45.5]): Place ▸ Via, click each. 🟦
- [ ] **C8** On the second "eyeball what the checker can't catch" warn ([50]), name the recurring pattern ("same move as the schematic"). 🟨

### DRC_GERBER
- [ ] **C9** Reframe the [03]/[06] "Check yourself" callouts as new scenarios instead of restating the prior prose. 🟨

### ASSEMBLY / BRINGUP
- [ ] **C10** Add a "do one drag-solder row with me" worked beat (tip temp, flux reload, good-vs-bridged) to ASSEMBLY. 🟨🟦
- [ ] **C11** Let ASSEMBLY own the "find the short before power" continuity teaching; have BRINGUP [01] recap it in one line instead of re-teaching. 🟨

---

## D. Gate / config / platform

- [x] **D1** ✅ Resolved via A2 — BOM_SOURCING `completionRef` set to `{kind:"none"}` and the "stripboard"-titled empty checklist deleted; no learner-facing stripboard wording remains. *(Optional future: seed real MPN/stock/second-source BOM-validation items if we want a soft gate here.)* 🟩
- [ ] **D2** Give BRINGUP a real evidence artifact instead of self-asserted `boardStatus` — require the bring-up measurements/log (already named in [16]) or a short VBUS-GND-open / TP1≈3.3 V / enumerates / LED2-blinks checklist. 🟨
- [ ] **D3** *(product, optional)* Add a tasteful PCBWay CTA at the LAYOUT board-setup step ([09]/[13]) and one assembly-consumables CTA (flux/SAC305/tip) in ASSEMBLY — one each, matching the restrained ORDERING model. Decline if it reads pushy. 🟩

---

## E. Assessment / quizzes

- [ ] **E1** Add SCHEMATIC quiz Q8 on the header both-ends/mislabel trap ("label module IO5 but header IO6 — does ERC catch it?" ✓ No). 🟨
- [ ] **E2** Replace the giveaway-absolute distractors ("…don't exist", "…out of stock", "labels are just notes") in REQUIREMENTS/BOM/SCHEMATIC quizzes with plausible-but-wrong reasons. 🟨
- [ ] **E3** Convert one ORDERING quiz item to a judgment question (all-through-hole board → is the ENIG upcharge worth it? ✓ Not necessarily). 🟨

---

## F. Nice-to-have (authored diagrams, not captures)

- [ ] **F1** BRINGUP rail-voltage decision-tree SVG (3.3→OK · 4.9→LDO passthrough · 0→short/open · dark LED→LED), per house diagram standards. 🟨
- [ ] **F2** A consolidated refdes "cast list" the learner can keep open across SCHEMATIC. 🟦

---

### Suggested order of execution
1. **A1** (hide placeholders) — unblocks everything else by making empty slots admin-only.
2. **A2 + A3 + D1** (stripboard removal + screenshot delete + BOM checklist) — the prod cleanups, in one pass.
3. **B1–B3** (board-setup trio) — the highest-ROI captures, hints already written.
4. **C5 + add captureHints (B4–B6, B13–B16)** — text edits that make the rest shoot-ready.
5. Then work down B (captures), C (content), E (quizzes) as time allows.
