# L1.01 "WROOM breakout" — focus panel v2 (student / teacher / engineer)

**Date:** 2026-06-12 · **Lesson:** `l1-01-wroom-breakout` @`v1` · **Method:** fresh dump of all 8 current stage cards (post the 2026-06-11 content batch), reviewed in parallel by three lenses — **Student** (true beginner), **Teacher** (instructional designer), **Engineer** (PCB SME, web-verified pinouts/dropout against datasheets). Follow-up to [the 2026-06-11 v1 panel](2026-06-11-l101-focus-panel.md).

Lens key: 🟦 Student · 🟨 Teacher · 🟧 Engineer.

---

## ✅ Applied 2026-06-12 (authorable items)

- **#3 "1:1 mirror" reword** (engineer, major) — SCHEMATIC [53] now says GPIOs mirror 1:1 and the rail positions (GND/+3V3/+5V) are convenience rails you add, not module pins.
- **#9 REQUIREMENTS [10] margin** — the ~50 mA headroom is now framed as "thin on paper, but fine here" instead of reading tight against the lead's "room to spare."
- **#8 quiz fixes** — BRINGUP Q3's "the battery is low" (there's no battery) → "That's normal — the rail sits near 5 V under light load"; SCHEMATIC Q5's two giveaway distractors replaced with plausible near-misses.
- **Cross-section [03]** — filled with a new authored SVG (`/guide-diagrams/two-layer-cross-section.svg`, to the diagram standards), so the foundational "top signals / bottom ground plane / vias" picture is no longer a blank student-facing slot.

**Held (deliberate, need a design decision — same risk class):** #4 BRINGUP self-asserted gate and #7 BOM completion (`none`). `ArtifactSubkind` has no clean learner-attachable BOM subkind, and changing gate wiring blind on prod could break the learner flow.
**Left for capture (you):** the LAYOUT board-setup trio + answer-key + keep-out + USB-pair + pour + floor-plan, DRC/Gerber screenshots, and the ASSEMBLY/BRINGUP bench clips.

---

## Headline

- **The content is in good shape and technically trustworthy.** The engineer found **no blockers** — every load-bearing value, pin, menu path, and quiz answer verified (D−=IO19/D+=IO20, strapping 0/3/45/46, ADC1=GPIO1–10, RT9080 ~0.31 V typ dropout, the diff-pair suffix rule, PWR_FLAG-not-on-+3V3, the GNDPWR trap). The v1 fixes landed cleanly: the header worked-example, the dropout wording, the keep-out recipe, SCHEMATIC Q8, the captured first-gesture clip.
- **The dominant remaining problem is now VISUAL — and it became student-facing.** Empty media is admin-only, so a student sees **nothing** in an unshot slot. The hardest stage (LAYOUT) is where almost every needed picture is currently an empty slot — and two of them (the cross-section and floor-plan) went empty when we deleted the "test screenshots." So a first-timer hits LAYOUT and the pictures vanish exactly where they're most lost. **All three lenses converge here.**
- **The difficulty ramp inverts** (teacher's headline): the conceptual peak (SCHEMATIC, 104 blocks) is the *lowest physical-risk* stage, while the irreversible hands-on stages (ASSEMBLY drag-solder, BRINGUP flash) are thin, lack a worked-example→fade, and their teaching clips are empty. The **drag-solder clip is the single highest-value missing visual.**
- **One real accuracy reword** (engineer, major): the "header pin N = module pin N, 1:1 mirror, no skip-list" claim overstates it — the rail positions (`+5V`, extra `GND`s, `+3V3`) are board convenience rails, not module pins (the module has no +5V pin).

---

## Cross-cutting findings (ranked)

| # | Issue | Lens(es) | Severity | Concrete fix |
|---|---|---|---|---|
| 1 | **LAYOUT is a visual black hole for students.** The board-setup click-path ([09]) has its 3 screenshots empty ([10][11][12]); the **answer-key** [55] is empty (caption literally says "check your placement against this"); the floor-plan [17] and cross-section [03] are empty (we deleted those as "test shots"); the keep-out rule-area [22], USB-pair [33], and ground-pour [38] are empty. Students see prose only, right where KiCad gets visual. | 🟦🟨 | blocker *(student)* | Fill the LAYOUT slots. Now that the capture tool works, the KiCad ones (board-setup trio, keep-out, USB-pair, pour, answer-key) are cheap screenshots; the cross-section [03] is an **authorable SVG** (quick win, no build); the floor-plan [17] a KiCad placement screenshot. Prioritize [10][11][12] + [55]. |
| 2 | **Ramp inverts: hands-on stages are under-scaffolded.** ASSEMBLY drag-solder ([08]–[09]) and BRINGUP flash ([12]–[13]) — the irreversible, hardware-destroying skills — get a recipe but **no worked-example→fade** (unlike SCHEMATIC/LAYOUT), and their teaching visuals (drag-pass clip, bridge-vs-clean macro, flash-dance clip) are empty. | 🟨🟦 | major | Add a "do one row/flash with me" worked walkthrough to each (mirror the diff-pair pattern); the **drag-solder clip** is the #1 visual to shoot (technique prose can't teach). Add a STEPS recipe for the enumerate→BOOT/EN→flash→LED2 chain. |
| 3 | **"1:1 mirror / header pin N = module pin N / no skip-list"** overstates the wiring — `+5V`, extra `GND`s, and `+3V3` header positions are board rails, not module pins (S3 module is 3V3-only, has no +5V pin). | 🟧 | major | Reword [53]/[58]/[59]: "every **GPIO** mirrors 1:1; the rail positions (GND/+3V3/+5V) are convenience rails you add." Keeps the artifact's intent matching the wiring. |
| 4 | **BRINGUP gate is self-asserted** (boardStatus BROUGHT_UP/QUARANTINED) with no evidence artifact, while easier upstream stages demand real ERC/DRC reports. A learner can mark "brought up" having proven nothing. | 🟨 | major | Require a minimal proof — a photo of ~3.3 V at TP1, or the LED2-blink clip / flash log — to back the status. (This was v1's deferred D2.) |
| 5 | **DRC_GERBER is picture-less** ([03] DRC dialog, [07] Gerber viewer both empty). "Open the Gerbers in a viewer and spot a mirrored layer" asks a beginner to recognize a defect in a tool they've never seen. | 🟦🟨 | major | Fill [07] (a labeled Gerber-viewer screenshot of "normal") and [03] (DRC at 0) — cheap KiCad/GerbView screenshots. |
| 6 | **The 44-pin header march** ([55]/[58]–[59]) is the cognitive-load spike: 2×22 rows, 5 ⚠ reused-net exceptions, and numbering that "jumps" (IO7→IO15) where Insert won't auto-fill — and [62] warns a mislabel passes ERC silently. The seams are exactly where a beginner mis-numbers unseen. | 🟦 | minor | Visually flag the jump rows in the table (where Insert stops helping) so the per-row cue lands where the silent-fail risk is. |
| 7 | **BOM_SOURCING now has no completion evidence at all** (completionRef `none` after stripboard removal) — the stage teaching the course's central habit (vague value → orderable MPN) is ungated, and the quiz isn't required. | 🟨 | minor | Make the quiz the soft gate, or require the BOM attachment as a non-blocking artifact (the BOM is the literal input to SCHEMATIC). |
| 8 | **Quiz polish:** BRINGUP Q3 distractor "the battery is low" — *there is no battery on this board* (giveaway + slightly misleading); SCHEMATIC Q5 ("what are the two buttons for") is recall-of-a-label, not transfer; a few ASSEMBLY/BRINGUP distractors are non-plausible throwaways. | 🟨 | minor | Swap battery-style throwaways for plausible near-misses (e.g. BRINGUP Q3: "a shorted output cap"); make SCHEMATIC Q5 a transfer item ("EN does nothing on reset — which net is suspect?"). |
| 9 | **REQUIREMENTS deepDive [10] coherence:** "~550 mA against 600 mA → ~50 mA margin" reads *tight* to a beginner, lightly contradicting the lead's "room to spare." | 🟨🟧 | minor | Reconcile: state that ~50 mA is the *intended, acceptable* margin and why, so the worked numbers and the headroom narrative agree. |
| 10 | **Scary-jargon first-contact:** BOM [00]/[01] drops `RC0805FR-075K1L` / `MPN` before the collapsed [05] deep-dive decodes them — an opaque string as the student's first "part." | 🟦 | minor | Half-sentence in [02]: "that string looks like noise now — [05] breaks it apart." |

**Engineer "verified correct" (do not churn):** USB pins, strapping set, ADC1/2, diff-pair naming, PWR_FLAG logic, GNDPWR trap, RT9080 dropout + USB-sag math, LED current math, PCBWay minimums + net classes, antenna keep-out + the "DRC won't flag a missing keep-out" caveat, ESD-at-the-port routing, hidden-GND-pin handling, all KiCad-10 menu paths, the assembly heat-order, and the 4.9 V-rail diagnosis. The lesson is technically sound.

---

## What improved since v1 (verified)

The 2026-06-11 batch landed and holds up: header "first column with me" worked example + Insert clip slot, find-unused-pins-via-ERC, **RT9080 dropout 0.31 typ/0.53 max** (engineer-confirmed), keep-out Rule-Area recipe, the "checker is blind to intent" naming, **SCHEMATIC Q8** (header mislabel trap, engineer-confirmed correct), the ORDERING ENIG-judgment question, and the **first-gesture clip is now captured** (`.mp4`). Stripboard is gone. So the v1 *content* asks are largely closed — the panel's weight has shifted almost entirely to **filling the visual slots** and the **hands-on-stage scaffolding**.

---

## Top priorities (impact ÷ effort)

1. **Fill the LAYOUT visual slots** — board-setup trio [10][11][12] + answer-key [55] first (student blockers), then keep-out [22], USB-pair [33], pour [38], floor-plan [17]. Mostly cheap KiCad screenshots now that capture works.
2. **Author the cross-section [03] SVG** — quick win, no build, restores the foundational "top signal / bottom plane" picture we emptied.
3. **Reword the "1:1 mirror" claim** (#3) — fast content edit, the one real accuracy item.
4. **DRC_GERBER screenshots** [03]/[07] — cheap, unblocks a picture-less stage.
5. **ASSEMBLY/BRINGUP scaffolding** — drag-solder worked beat + clip (highest-value clip), flash STEPS + clip, and a BRINGUP evidence artifact. (Clips need the physical build; the STEPS + gate change are authorable now.)
6. **Quiz + coherence polish** — BRINGUP Q3 "battery" distractor, SCHEMATIC Q5, REQUIREMENTS [10] margin framing, BOM soft-gate.

**Meta-note:** deleting the LAYOUT [03]/[17] "test screenshots" earlier removed visuals that — because empty media is admin-only — students now experience as blank. The deletion was correct (they were throwaway captures), but those two slots still need real content (an SVG for [03], a fresh capture for [17]).
