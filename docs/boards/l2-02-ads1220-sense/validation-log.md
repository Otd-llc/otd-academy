# ADS1220 Precision Sense (L2.02) — validation log

> Evidence trail for the **Recursive Board-Design Validation Protocol**
> (`../_protocol.md`). One entry per pass. This log backs the board's
> `DESIGN_VALIDATION` attestations — when the owner ticks "each IC
> datasheet-verified", the proof is here.
>
> **Provenance of the datasheet facts.** Every ADS1220 number in `design.md` was read
> **out of the actual PDF** — TI **SBAS501D, revised May 2026** — page by page (§5 pin
> table, §6.1–6.9 specs and typical characteristics, §7.1 + Tables 7-1…7-8, §8.3.1–8.3.6,
> §9.1–9.4), not from a product page, a summary, or memory. The same was done for the
> **LP5907 (SNVS798Q, rev. July 2025)** and the **RT9080 (DS9080-09, Dec 2024)**; ESP32-S3
> logic levels came from the Espressif datasheet; the thin-film thermal-EMF figure from
> Vishay's thin-film chip-resistor documentation. Stock/lifecycle came from a **live
> DigiKey API screen on 2026-07-30** run through the repo's own `scripts/digikey-stock.ts`;
> catalog strings and the project row came from a **read-only query of the live database**.
> Per the protocol, no load-bearing claim was folded in from a delegated summary without
> being re-checked at source — and Pass 12 records one claim that a machine check
> **falsified**.

| | |
| --- | --- |
| **Slug** | `l2-02-ads1220-sense` |
| **Status** | **`DRY ✓ (design-stage part-ready)`** |
| **Passes run** | 14 |
| **Last dry pass** | Pass 14 (2026-07-30) — zero new material findings |

## Gate (Definition of done — `[D]` design-stage)

- [x] Requirements traced (F1–F9 → net → K-row → BOM line → risk) · **every pin of the
      ADS1220 accounted for** (§6 pin table, all 16) · power-up/down sequencing proven (K32/K33)
- [x] Every number worst-case-proven (K1–K37, min/max/temperature) · both new active parts
      datasheet-verified against their **own** datasheets
- [x] Power integrity proven (K29–K31: 19× current headroom, 26 mW, C<sub>OUT</sub> inside
      0.7–10 µF at every corner) · every failure mode mitigated-or-accepted (RK1–RK11)
- [x] Every part hand-buildable within the L2 envelope (K2: PW mandatory, VQFN excluded) ·
      sourceable with exact import strings (26 lines / 64 placements parsed and
      strict-matched: 0 errors, 0 qty mismatches, 18 matched, 8 new)
- [x] Layout constraints captured (RK6-layout, RK9, RK13, RK14 → `[L]`) · footprint↔pinout
      intent captured (RK12 → `[S]`) · teachable (§7 pedagogy block) · internally
      consistent · pipeline-conformant
- [x] **Conditional audits:** `hasMainsNet` / `hasLiIon` / `hasThermalConcern` /
      `requiresStripboard` all false and all four **re-verified against this design** — none
      fires, **none needs changing**. **RF / regulatory FIRES on the board's nature** and was
      run as Pass 10
- [x] Every risk de-risked or explicitly scheduled to `[S]`/`[L]`/BRINGUP
- [x] **14 passes run AND a `[D]` dry pass achieved** (Pass 14)
- [x] `validation-log.md` complete

**Owed at later stages** (cannot honestly close pre-schematic/pre-layout, per protocol phasing):
`[S]` footprint ↔ symbol ↔ pinout pad-by-pad for U3 (PW **not** RVA), U4, the 0805
precision passives and J4, plus the final ESP32-S3 GPIO map and the exact MUX/VREF register
bit patterns (RK12) · `[L]` layout-readiness: solid unsplit plane, analog-island placement,
matched differential routing, Kelvin sense traces, thermal symmetry, antenna keep-out on all
four layers (RK6-layout, RK13, RK14) · `[L]` fab-DRU DRC · **BRINGUP** the empirical
Wi-Fi-on/off immunity test (RK9/K35), which is the *only* honest acceptance for the RF
conditional.

**Owner items surfaced (not design defects):** the live `REQUIREMENTS_REVIEW` checklist on
revision `v1` is boilerplate from other boards (4 of 5 items N/A) — replacements proposed in
`design.md` §7; and `Project.targetCost` is **null** — §8 proposes **$30**.

## Passes

### Pass 1 — Requirements & traceability + initial design (engineer)

| # | Sev | Finding | Verified? | Fix / decision | Re-proof |
| --- | --- | --- | --- | --- | --- |
| 1.1 | — | Read the live `l2-02-ads1220-sense` project row and its revision: SENSE/L2, `criticalPath=true`, **PREMIUM $4900**, `publishedRevisionId=null`, revision `v1` at stage `REQUIREMENTS`, `bomFrozenAt=null`, **zero BOM lines**, all four flags false, `targetCost=null`. `disciplineTaught` = "Sense: precision SPI ADC layout (24-bit ADS1220); low-noise reference + analog ground". DAG: `FOUNDATION` on l1-01; **`DE_RISK` for l3-de-ads1292r** → l3-01-eeg-front-end. | yes (DB, read-only) | Scope locked **from the DB, not invented**: L1.01 core reused whole + an ADS1220 precision front end. Nothing to break — no parts, no BOM, no published revision. Drafted F1–F9, E1–E4, I1–I5, the topology and the first calc trail. Every F-req traces requirement → net → K-row → BOM line → risk. | — |
| 1.2 | — | The DAG role is the design brief's real constraint: this board **de-risks the biopotential chain**, so what must transfer downstream is *differential-front-end discipline* (routing, grounding, supply/reference partitioning), not a parts list. | yes (DB edges + `scripts/populate-curriculum-dag.ts`) | Recorded in the header; used later as the tiebreaker in Pass 11 (11.2). | — |

**Residual:** scope + skeleton only; no datasheet numbers yet.

### Pass 2 — Part-truth / ADS1220 datasheet lock (engineer; read from the PDF) — **architecture-defining**

| # | Sev | Finding | Verified? | Fix / decision | Re-proof |
| --- | --- | --- | --- | --- | --- |
| 2.1 | **CRITICAL** | **A single-ended input on a unipolar 3.3 V supply cannot reach the board's headline claim.** §9.1.4: an AVSS-referenced signal requires `PGA_BYPASS`, and §8.3.2.2 states "The PGA is always enabled for gain settings greater than 4" — so single-ended caps gain at **4**. Table 7-3 (PGA disabled, 20 SPS, gain 4) gives **1.26 µV<sub>RMS</sub> / 3.91 µV<sub>pp</sub>**, **9.5× worse** than the gain-128 floor. §9.1.4 is explicit: "If gains larger than 4 are needed to measure a single-ended signal, the PGA must be enabled. In this case, a bipolar supply is required." | **yes (datasheet §8.3.2.2, §9.1.4, Tables 7-1/7-3)** | **The whole architecture follows from this one paragraph: the board is differential-only.** The on-board bridge exists to *supply* a differential signal at a legal common mode — it is not decoration. Added K9, RK1. | K9, K10, RK1, §1 F4, §2 |
| 2.2 | HIGH | **The datasheet's own bridge example uses the wrong input pair for a layout-teaching board.** §9.2.3 (reg `00h=3Eh`) uses AIN1/AIN2 so it can claim REFP1/REFN1's low-side switch (PSW) for bridge power-down. But §9.4.1 says "Best input combinations for differential measurements are AIN0, AIN1 and AIN2, AIN3", and Figs 6-16…6-19 *measure* AIN0/AIN1 as lowest absolute (≈ ±4 nA @25 °C) and lowest differential input current; AIN3 is worst (−25 nA @85 °C, **−250 nA @125 °C**, Fig 6-18 — the low-side switch). In the **PW** package AIN1 = pin 10 and AIN2 = pin 7, i.e. **opposite sides of the body**. | **yes (datasheet §9.2.3, §9.4.1, Figs 6-16…6-20, Table 5-1)** | **Deviated from TI's worked example on purpose:** signal on **AIN0/AIN1** (pins 11/10, adjacent), reference on **REFP0/REFN0** (pins 9/8, adjacent), aux on AIN2/AIN3. All four bridge wires now land on four adjacent pins. Cost, accepted and stated: **no PSW bridge power-down**. Added K20, RK4. | K20, RK4, §2, §6 pin table |
| 2.3 | HIGH | **Nominal FSR overstates the usable range.** Eq 5 gives FSR = ±V<sub>REF</sub>/Gain = ±25.8 mV at V<sub>REF</sub> = 3.3 V, G = 128 — but §6.3 note 2 and §9.2.3 Eq 40 cap V<sub>IN</sub> at ±[(AVDD−AVSS) − 0.4]/Gain = **±22.66 mV**, because each PGA output must stay 200 mV off the rails (§8.3.2.1 Eq 6). Quoting FSR alone overstates by **12 %**. | yes (datasheet) | Split K6 (nominal) from **K7 (binding)** and checked every input budget against K7, not K6. | K6, K7, K8, K14, K16 |
| 2.4 | MED | **NMRR is specified with an *external* clock.** §6.5 lists 105 dB (50 Hz ±3 %, bit=10b) / 105 dB (60 Hz ±3 %, 11b) / **90 dB (50 *or* 60 Hz ±3 %, 01b)** — all "external CLK". The internal oscillator is **±2 %** and §8.3.6 says the notches "vary by the same amount". | yes (datasheet §6.5, §8.3.6, Fig 6-13) | Worked the worst case rather than fitting a crystal: a 2 % clock error is indistinguishable from a 2 % tone offset, so **2 % + ~0.5 % grid deviation = 2.5 % sits inside the ±3 % band** where ≥90 dB is guaranteed — **margin 0.5 percentage points**. CLK tied to DGND; external 4.096 MHz named as the production upgrade and deliberately not fitted (routing a 4 MHz square wave into the analog island to buy 0.5 pp is a bad trade). Added K28, RK8. | K28, RK8 |
| 2.5 | — | Locked the rest: PW TSSOP-16 has **no thermal pad** (Table 5-1) while RVA VQFN does; 47 Ω on all five digital lines is TI's own §9.1.1 recommendation; CLK→DGND selects the internal oscillator (§8.3.4/§9.1.5); AIN3/REFN1 must be left floating when unused (§9.1.5); f<sub>MOD</sub> = 256 kHz normal (§8.3.5 Table 8-3); SPI mode 1 (§9.1.1); internal reference 2.045/2.048/2.051 V, 5 typ / **30 max** ppm/°C, 110 ppm per 1000 h (§6.5); shorted-input self-test via `MUX[3:0]=1110b` (§9.1.6); the internal EMI filter is 200 Ω + 25 pF ⇒ 31.8 MHz — **independently re-derived and it matches the figure §9.1.2 states**. | yes (datasheet) | Folded into K1–K5, K17, K21, K26–K28. | K1–K5 |

**Residual:** the ADS1220 is locked; the supply/reference/ground architecture around it is not yet attacked.

### Pass 3 — Sourcing / lifecycle + strict-import check (engineer; live DigiKey + live catalog)

| # | Sev | Finding | Verified? | Fix / decision | Re-proof |
| --- | --- | --- | --- | --- | --- |
| 3.1 | — | Live DigiKey screen 2026-07-30 (all **Active**): `ADS1220IPWR` 4,107 @ $9.26 · `LP5907MFX-3.3/NOPB` 210,924 @ $0.66 · `TPS7A2033PDBVR` 31,923 @ $0.35 · `RT0805BRD071KL` 11,443 @ $0.10 · `RT0805BRD071ML` **1,073** @ $0.10 · `RC0805FR-071ML` 130,175 @ $0.10 · `RC0805FR-0747RL` 207,979 @ $0.10 · `RC0805FR-0710RL` 159,538 @ $0.11 · `CL21C103JBFNNNE` 66,126 @ $0.41 · `C0805C102J5GACTU` 489,212 @ $0.16. | yes (live DigiKey API) | Recorded in §8. | — |
| 3.2 | MED | **Two candidate parts rejected on live data, not on preference.** `ERA-6AEB102V` (Panasonic 1 kΩ 0.1 %) returned **0 stock**. Samsung `CL21C103JBANNNC` / `CL21C222JBANNNC` / `CL21C102JBANNNC` and `C2012C0G1H103J125AA` **do not resolve at all** — the C0G part numbers guessed from the series pattern are not real orderables. | yes (live DigiKey API) | Every C0G value re-picked from parts that actually resolve: 10 nF = Samsung `CL21C103JBFNNNE`, 1 nF = KEMET `C0805C102J5GACTU`. **A guessed MPN would have passed a human read and failed the strict import.** | §8 |
| 3.3 | MED | Reused-line audit against the live `Part` table: **all 18 reuse strings match byte-for-byte**, including `Würth Elektronik` (ü) and `Samsung Electro-Mechanics`. | yes (Prisma read) | Confirmed the CSV can import without a manufacturer-string edit. | bom.csv |
| 3.4 | LOW | The LP5907 datasheet opens with "For a more updated portfolio device, see the **TPS7A20**" — a portfolio-preference note that reads like a lifecycle warning. | yes (datasheet §1 + DigiKey lifecycle) | Checked at the distributor rather than assuming: LP5907MFX-3.3/NOPB is **Active with 210,924 in stock**. Kept it (better published noise/PSRR at the frequencies that matter here) and named **TPS7A2033PDBVR** — same SOT-23-5, Active, 31,923 — as the second source. | §4, §8 |

**Residual:** design now goes to the adversarial passes (4–12).

### Pass 4 — Math audit (adversarial, fresh eyes)

| # | Sev | Finding | Verified? | Fix / decision | Re-proof |
| --- | --- | --- | --- | --- | --- |
| 4.1 | MED | The differential corner of the RC network was computed from C<sub>DIF</sub> alone. The two common-mode caps appear **in series across the pair**, adding C<sub>CM</sub>/2 — ignoring it **understates the capacitance by 5 %** and overstates the corner. | yes (re-derived) | K17 now uses f<sub>DIFF</sub> = 1/(2π·2R<sub>F</sub>·(C<sub>DIF</sub>+C<sub>CM</sub>/2)) = **7.58 kHz** and states the term explicitly. | K17 |
| 4.2 | MED | Johnson-noise budget used the source resistance only. The **bridge's own 1 kΩ differential source impedance** (K13) is in series with the two 1 kΩ filter resistors — the correct total is **3 kΩ**, not 2 kΩ. | yes (re-derived) | K19 corrected: 7.05 nV/√Hz × √20 Hz = **31.5 nV<sub>RMS</sub>** → total 95.4 nV, **+6.0 %** on the ADC's 90 nV floor. | K19, K13 |
| 4.3 | MED | The noise bandwidth used for K19 was asserted rather than derived. A sinc¹ decimating to 20 SPS has ENBW = DR/2 = **10 Hz**, not 20 Hz. | yes (re-derived) | **Kept the conservative 20 Hz and said so**, noting it over-states the resistor contribution by √2. Worst-casing in the direction that hurts is the protocol's rule. | K19 |
| 4.4 | MED | The 824 µV step was first sized with the approximation δ ≈ −10⁶/R<sub>s</sub>. Exact: R<sub>16</sub>‖R<sub>17</sub> = 1000·10⁶/(10⁶+1000) = **999.001 Ω**; ΔV = 3.3·[999.001/1999.001 − 0.5] = **−824.3 µV**. | yes (re-derived exactly) | K16 carries the exact figure and its **±1.3 µV (±0.16 %)** uncertainty from two ±0.1 % parts. | K16 |
| 4.5 | MED | The claimed noise-free resolution at V<sub>REF</sub> = 3.3 V was scaled from Table 7-2 without a cross-check. Re-derived from §7.1 Eq 2 independently: ln[2·3.3/(128·0.41 µV)]/ln2 = **16.94 bits**, and Table 7-2's 16.26 bits at 2.048 V plus log₂(3.3/2.048) = 0.688 bits gives **16.95** — the two routes agree. | yes (two independent derivations) | K10 states both the table value and the rescaled value. | K10, K11 |
| 4.6 | LOW | Bridge initial imbalance was estimated at "a few mV" without a worst case. Four independent ±0.1 % parts give ΔV ≤ (AVDD/2)·2×10⁻³ = **±3.3 mV**; at gain 128 the PGA outputs sit at 1.65 ± 0.21 V, inside 0.2…3.1 V with 1.4 V spare. | yes (re-derived) | K14 quantified; confirms the offset calibration is sufficient and the PGA does not saturate. | K14 |
| 4.7 | OK | K7/K8/K11/K12/K22/K26/K29/K34 re-derived independently — LSB 3.07 nV, V<sub>CM</sub> window 0.825–2.678 V, 2.72 mW/resistor, cable-drop 364 ppm / 1.4 ppm/°C, 6.67 MHz SCLK ceiling, 25.9 mW / ΔT 5.0 °C, plane-vs-trace 40 nV vs 950 nV — all reproduce. Units checked throughout. | yes | confirmed | — |

**Residual:** the equations are right; what they assume away is not yet attacked (Pass 5).

### Pass 5 — Physics / first-principles (adversarial, fresh eyes)

| # | Sev | Finding | Verified? | Fix / decision | Re-proof |
| --- | --- | --- | --- | --- | --- |
| 5.1 | **HIGH** | **The 2.4 GHz rejection claim was off by 42 dB.** The first calculation treated the filter caps as ideal and produced ≈ −117 dB at 2.4 GHz. A real 0805 MLCC is **far above its self-resonance** there: with ESL ≈ 0.9 nH, \|Z\| = 2π·2.4 GHz·0.9 nH = **13.6 Ω**, so the external RC delivers 13.6/(1000+13.6) = **−37.4 dB**. Adding the ADS1220's internal 200 Ω/25 pF EMI filter (\|Z<sub>C</sub>\| = 2.65 Ω ⇒ −37.6 dB) gives **≈ −75 dB total**. | yes (re-derived; internal filter corner independently reproduced as 31.8 MHz, matching §9.1.2) | Rewrote K18 around the ESL-limited number and **removed the ideal-cap claim entirely**. This directly changed Pass 10's conclusion: with only −75 dB (not −117 dB) available, the RF conditional cannot be closed analytically and needs an empirical acceptance test (K35). | K18, K35, RK9 |
| 5.2 | **HIGH** | **Thermal EMF was missing from the design entirely.** At a 0.41 µV<sub>pp</sub> floor, every dissimilar-metal junction in the signal path is a thermocouple; a fraction of a degree of gradient across the differential pair is comparable to the whole noise band. Nothing in the draft addressed it. | yes (mechanism; Vishay specifies thin-film chip resistors at **< 0.1 µV/°C** — a figure thick-film datasheets do not carry at all) | Added the constraint to §1, made it **RK6**, and turned it into two concrete decisions: **thin film throughout the signal path** (already chosen for tolerance/tempco — now justified on a third axis) and a **thermal-symmetry `[L]` rule** (identical junction counts per leg, R18/R19 placed adjacent and thermally coupled, analog island away from U2/U4/WROOM, no single-sided airflow). The ADS1220's own §9.4.1 asks for the same discipline around a cold junction. | §1, RK6, §4 |
| 5.3 | **HIGH** | **The on-board bridge is ~200× less stable than the ADC, and the draft's teaching arc did not know it.** Four discrete ±25 ppm/°C resistors track to at worst 50 ppm/°C, giving (AVDD/2)·50 ppm = **±82.5 µV/°C** of differential drift — 201× the 0.41 µV<sub>pp</sub> floor **per degree**. A learner told to "watch the bridge read microvolts" would conclude the ADC was broken. | yes (re-derived) | Added **K15** and **RK5**, and **reframed the board's µV proof**: F3 is now the **shorted-input measurement** (`MUX[3:0]=1110b`, §9.1.6), which is immune to the source. The bridge became the *second* experiment and its drift became **the lesson**. Considered and rejected a monolithic matched network (≈5 ppm/°C tracking): no hand-solderable 0805-class 4-element array, and hiding the effect would delete the best teaching moment on the board. | K15, RK5, §1 F3, §2, §7 |
| 5.4 | MED | **A ferrite bead on DVDD would have built an amplifier.** A BLM-class bead (~1 µH at low frequency, DCR 0.38 Ω) with the 1 µF local cap resonates at 1/(2π√(LC)) = **159 kHz** with Q = (1/R)√(L/C) ≈ **2.6 — about 8 dB of gain** exactly where attenuation was wanted. | yes (re-derived) | **Replaced the ferrite with R7 = 10 Ω.** A resistor cannot peak; f<sub>c</sub> = 15.9 kHz with C11, DC drop 1.1 mV at I<sub>DVDD(max)</sub>, and a 3 mA DOUT bit droops C11 by 0.75 mV. Recorded as K24 and kept as a teardown aside. | K24, §2 |
| 5.5 | MED | The "separate low-noise LDO" justification was hand-waved as "less noise". Checked against the ADS1220's actual PSRR (80 dB min / 105 typ at dc; Fig 6-14 shows **120–140 dB at gain 128** across 0.1–1000 kHz): via PSRR, even the RT9080's 42 µV<sub>RMS</sub> lands at **~4 nV** input-referred — **irrelevant**. | yes (datasheet Fig 6-14, §6.5) | **Rewrote the justification honestly** (§5). The LDO earns its place on two *other* paths: (a) AVDD **is** the bridge excitation, and a 0.1 %-imbalanced bridge converts supply noise to differential at that ratio — 42 nV (RT9080) vs 6.5 nV (LP5907), i.e. ~47 % vs ~7 % of the ADC's own floor, and this cancels in **ratiometric** mode but not in internal-reference mode; (b) a separate regulator keeps the analog **return current** inside the analog island. The RT9080 is therefore a genuine second source at a **quantified** cost. | §5, §4, §8 |
| 5.6 | MED | Ground bounce was asserted ("keep the returns separate") with no number. | yes (re-derived from 1 oz Cu = 0.5 mΩ/sq and the §6.5 CMRR spec) | **K34 now puts volts on it.** Solid plane (3–5 squares ⇒ 1.5–2.5 mΩ): 500 mA ⇒ 0.75–1.25 mV ⇒ **24–40 nV** input-referred at 90 dB CMRR — under the 90 nV floor. A 0.25 mm × 30 mm return trace (120 squares ⇒ 60 mΩ): 30 mV ⇒ **950 nV** — 2.3× the entire noise band. **A factor of ~24**, and the headline number of the layout lesson. | K34, RK14, §2 |
| 5.7 | MED | The anti-alias corner (7.58 kHz) is **38× above** even TI's relaxed "data rate × 10" guidance (§9.1.2 ⇒ 200 Hz). Left unexplained this reads as a miss. | yes (re-derived the alternatives) | **Bounded and stated rather than quietly accepted** (K17a): reaching 200 Hz needs ~400 nF differential at R<sub>F</sub> = 1 kΩ, which **C0G cannot supply in 0805** — and §9.4.1 forbids X7R for the differential cap. Raising R<sub>F</sub> to 1.8 kΩ with 22 nF C0G reaches 1.9 kHz but costs +10 % noise, thinner stock and +$2.20. Decisive counter-argument: **the digital filter attenuates everything between its notches**, so aliasing is a *narrow-band* threat around n·f<sub>MOD</sub>, and this board has **no switching regulator** to put energy there. −30.6 dB accepted, with the reasoning on the page. | K17, K17a |
| 5.8 | OK | Charge-injection/settling after the SW3 press (τ = 15.8 µs ≪ the 50 ms conversion), the 1 MΩ step network's contribution to the 500 Ω node (0.064 nV/√Hz, ×5×10⁻⁴ coupling), the 1 MΩ bias resistors' loading of a 350 Ω source, and the excitation-return DC offset across the plane (13.3 mA × 2 mΩ = 27 µV common-mode ⇒ 0.85 pV input-referred) all check out. | yes | confirmed | — |

**Residual:** the companion parts' own datasheets not yet independently attacked (Pass 6).

### Pass 6 — Part-truth for the companion parts (adversarial, fresh eyes)

| # | Sev | Finding | Verified? | Fix / decision | Re-proof |
| --- | --- | --- | --- | --- | --- |
| 6.1 | **HIGH** | **The LP5907 has a maximum output capacitance, and the reflex "add 10 µF of bulk to the analog rail" would break the loop.** §5.6: C<sub>OUT</sub> min 0.7 µF, typ 1 µF, **max 10 µF**, ESR 5–500 mΩ. | yes (datasheet §5.6) | Board AVDD net held to **C9 1 µF + C10 100 nF = 1.1 µF**. Worst-cased both ends: at the −55/+125 °C X7R extreme with −10 % tolerance the net is **0.85 µF (21 % over the 0.7 µF floor)**; over the real 0–50 °C range **0.95 µF (36 % over)**. A 50 V X7R at 3.3 V has negligible DC-bias derating. Added **K31** and **RK11**, and made "do not add bulk to AVDD" an explicit schematic/guide rule. | K31, RK11 |
| 6.2 | MED | The LP5907's start-up was assumed benign. §5.5: t<sub>ON</sub> = **80 µs typ / 150 µs max** from EN to 95 % of V<sub>OUT</sub> ⇒ **≈ 39 V/ms** — roughly **2× faster** than the ADS1220's §9.3.2 requirement of "slower than 1 V per 50 µs" (20 V/ms). | yes (both datasheets) | Escalated into Pass 8 (8.2). Also noted the *useful* half of §5.5: EN-controlled start-up has **1 % overshoot** versus 5 % when the LDO tracks V<sub>IN</sub> — another reason to gate EN. | K33, K32 |
| 6.3 | MED | The analog LDO was drawn from the 3V3 rail in the first sketch — **no dropout headroom** (LP5907 needs V<sub>IN</sub> above V<sub>OUT</sub>; a 3.3 V→3.3 V LDO does not regulate). | yes (datasheet §5.5) | **Moved U4's input to VBUS** (downstream of F1/D1, so it inherits L1.01's over-current and ESD protection). This also produced a *better* result than the fix required: the analog rail is now generated and returned independently of the digital rail (K30, and it strengthens K34). | K30, §2, §5 |
| 6.4 | LOW | Assumed the LP5907 (SOT-23-5 DBV) and RT9080 (TSOT-23-5) would have different pinouts — a classic second-source trap. | yes (both datasheets, side by side) | **They are identical**: 1 = IN/VIN, 2 = GND, 3 = EN, 4 = NC, 5 = OUT/VOUT. Recorded in §6's `[S]` capture, and it is what makes the RT9080 a genuine drop-in second source (at 42 vs 6.5 µV<sub>RMS</sub>). | §4, §6, §8 |
| 6.5 | MED | Logic-level compatibility was assumed rather than checked in **both** directions. | yes (ESP32-S3 datasheet + ADS1220 §6.5) | K25 now proves all four: ESP32→ADC **330 mV / 660 mV**; ADC→ESP32 **165 mV / 165 mV** (the tighter direction, and specified at I<sub>OH</sub> = 3 mA against a real 50 nA load, so the true margin is larger). Worst case includes R7's 1.1 mV DVDD drop, because the ADS1220's thresholds scale with DVDD. | K25 |
| 6.6 | LOW | The LP5907's V<sub>IN</sub> abs-max is **6 V** — lower than the RT9080's 6.5 V — and it sits on VBUS. | yes (datasheet §5.1) | Same exposure L1.01 already carries and accepted for the RT9080 (VBUS is PTC-fused and USBLC6-clamped). No new mitigation; recorded so it is not rediscovered as a surprise. | §4 |

**Residual:** failure modes on the exposed analog channel not yet enumerated (Pass 7).

### Pass 7 — FMEA / failure modes (adversarial, fresh eyes) — **two design changes**

| # | Sev | Finding | Verified? | Fix / decision | Re-proof |
| --- | --- | --- | --- | --- | --- |
| 7.1 | **HIGH** | **The reflex ESD part would have destroyed the measurement.** The draft carried a CDSOD323-T05C on each aux input (the part L1.03 and L1.05 both use). Its leakage is **≤ 5 µA at the 5 V spec point**; across the 1 kΩ series resistor that is **≤ 5 mV of offset — 12 000× the 0.41 µV<sub>pp</sub> noise floor**, and it drifts with temperature. | **yes (Bourns datasheet + re-derived)** | **DESIGN CHANGE: removed all TVS diodes from the analog inputs.** Protection is now R22/R23 (1 kΩ) holding every plausible fault under the ±10 mA abs-max, the chip's own ±2 kV HBM structures (raised ~1.7× by the series R, K37), silk, and handling discipline. Considered and rejected a low-leakage clamp (BAV199-class, ~3 nA ⇒ 3 µV) as still 7× the floor for a benefit the 1 kΩ already provides. **This is the first OTD board where fitting the standard protection part is the error** — recorded prominently in §4 so it is not "corrected" later. | K36, K37, RK7, §4 |
| 7.2 | **HIGH** | **A floating source on the aux channel has no DC bias path.** A thermocouple (the canonical ADS1220 sensor) is galvanically isolated; with only series resistors and caps, the PGA's common mode drifts out of the K8 window and the channel returns garbage with no error indication. | yes (§8.3.2.1 Eqs 12–14 + mechanism) | **DESIGN CHANGE: added R24 (1 MΩ, AIN2→AVDD) and R25 (1 MΩ, AIN3→AGND)** — the classic two-resistor bias, which sets V<sub>CM</sub> = AVDD/2 by symmetry, draws 1.65 µA, and costs 0.58 mV of offset on a 350 Ω source. **Free bonus:** with nothing plugged in the channel rails to full scale ⇒ **open-sensor detection**, and both rails are inside the AVSS−0.1…AVDD+0.1 legal range (§6.3) so §8.3.1's "overdriving an unused input" warning is not tripped. Added K23. | K23, RK7, §2 |
| 7.3 | MED | **Aux input driven while AVDD is off** (a consequence of the Pass-8 EN-gating change): analog inputs are abs-max'd to AVDD+0.3, so with AVDD = 0 any external 3.3 V violates it. | yes (re-derived) | Bounded rather than assumed: the internal ESD diode conducts (3.3−0.7)/1 kΩ = **2.6 mA**, well under the **±10 mA** continuous abs-max (§6.1), and back-feeds AVDD through the LP5907's internal **230 Ω** discharge pulldown to ~0.6 V. **Safe.** Recorded in K36. | K36 |
| 7.4 | MED | **AIN3/REFN1's internal low-side switch on an exposed header.** §9.1.5 warns to leave that pin floating when unused "to avoid accidentally shorting the pin to AVSS through the internal low-side switch". | yes (datasheet §9.1.5, §6.5 R<sub>ON</sub> 3.5/5.5 Ω, 30 mA) | PSW is left at 0. A spurious closure with a sensor attached draws ≤ 3.3 mA through R23 — under both the 30 mA switch rating and the ±10 mA pin limit. When nothing is attached the pin is biased through 1 MΩ, so a closure draws ≤3.3 µA. **Bounded.** | K23, K36, RK7 |
| 7.5 | MED | The 824 µV step network hangs a 1 MΩ resistor and a mechanical switch off a µV-precision node — a potential antenna and leakage path. | yes (re-derived) | Coupling is divided by the 500 Ω/1 MΩ ratio (**5×10⁻⁴**); the 1 MΩ's own Johnson noise reaches the node as 0.064 nV/√Hz; open-switch leakage is >TΩ; SW3's ~0.1 Ω contact resistance in series with 1 MΩ is a **0.1 ppm** error on the step. **Negligible on every axis.** | K16 |
| 7.6 | OK | Reverse-polarity (USB-C keyed), hot-plug, VBUS over-current (F1), USB ESD (D1), SPI stall (K27's 54.7 ms timeout cannot fire with a real CS), and PGA saturation from bridge imbalance (K14) all correctly handled or inherited. | yes | confirmed | — |

**Residual:** the two-rail power architecture and its sequencing not yet audited (Pass 8).

### Pass 8 — Power integrity + sequencing (adversarial, fresh eyes) — **design change**

| # | Sev | Finding | Verified? | Fix / decision | Re-proof |
| --- | --- | --- | --- | --- | --- |
| 8.1 | **HIGH** | **Two independently-enabled LDOs on one VBUS have no defined power-up order, and the ADS1220 cares.** §9.3.1: "Ramping DVDD together with or before AVDD minimizes any leakage current through AIN3/REFN1 because of the low-side switch connected to this input. **If AVDD ramps before DVDD, then the low-side switch is in an unknown state and can short the AIN3/REFN1 input to AVSS** until DVDD has ramped." With U4's EN tied to its own V<sub>IN</sub>, the order is set by two unrelated LDO start-up times — i.e. undefined. | **yes (datasheet §9.3.1)** | **DESIGN CHANGE: U4's EN is driven by an ESP32 GPIO (GPIO4), and the LP5907's internal 1 MΩ pulldown makes AVDD default OFF.** AVDD therefore *cannot* precede DVDD — the hazard is designed out in hardware, not documented around. Three further wins fell out: firmware gets a **power-cycle recovery path** (drive EN low; the LDO actively discharges through 230 Ω), start-up overshoot drops from 5 % to **1 %** (§5.5), and the analog rail becomes firmware-controllable for a low-power lesson. Added K32; F8 became a testable requirement (TP3 reads 0 V before firmware asserts it). | K32, RK3, §1 F8, §2 |
| 8.2 | MED | **§9.3.2's ramp-rate requirement cannot be met.** "The power-supply ramp rate must be monotonic and slower than 1V per 50µs" (**20 V/ms**); the LP5907 does ≈ **39 V/ms** (6.2) and the VBUS-tracking RT9080 rail is faster still. Slowing AVDD is impractical (13.3 mA load; C<sub>OUT</sub> is capped at 10 µF by 6.1). A 220 Ω series resistor on DVDD *would* bring that rail inside 20 V/ms but costs 40 mV of logic margin and fixes only half the problem. | yes (both datasheets; alternatives re-derived) | **Accepted with a proven mitigation rather than a cosmetic one.** TI's own §9.1.6 sequence mandates a **RESET (06h)** after power-up **regardless of ramp rate** — "to make sure the device is properly reset after power-up" — followed by a **register read-back (RREG 23h)**. Both are made mandatory here, with a GPIO4 power-cycle retry on mismatch. The DVDD series resistor stayed at **10 Ω** (filtering, K24) because 220 Ω would have bought a partial fix at a real cost. Added K33. | K33, RK3, §2 |
| 8.3 | MED | The AVDD budget omitted two datasheet adders: the **+70 µA typ** when an external reference is selected (our normal mode) and the external cell's excitation current. | yes (datasheet §6.5 note 2) | K29 rebuilt: bridge 3.3 mA + 350 Ω cell 9.4 mA + I<sub>AVDD</sub> 0.58 mA (510 µA + 70 µA) = **13.3 mA** against 250 mA — **19× headroom**; 25.9 mW at V<sub>BUS</sub> 5.25 V; ΔT = 5.0 °C on θ<sub>JA</sub> 193.4 °C/W. | K29, K4 |
| 8.4 | LOW | Decoupling was specified as "100 nF somewhere near". The pin table is more specific: AVDD "Connect a 100nF (or larger) capacitor to **AVSS**"; DVDD "…to **DGND**"; §9.3.3 adds "avoiding the use of vias for connecting the capacitors to the device pins can offer good noise immunity" and "Connect analog and digital ground together as close to the device as possible". | yes (datasheet Table 5-1, §9.3.3) | C10 and C12 pinned to pins 12 and 13 respectively in §6's capture; the via-light placement rule folded into RK14. | §6, RK14 |
| 8.5 | OK | Thermal: largest new dissipator 26 mW (ΔT 5 °C); largest on the board still L1.01's RT9080; bridge resistors at 2 % of rating. **`hasThermalConcern=false` verified, not inherited.** Short-circuit path bounded by I<sub>SC</sub> 250–500 mA into the LP5907's 160 °C thermal shutdown. | yes | confirmed — deep-thermal conditional does **not** fire | §5 |

**Residual:** buildability against the L2 skill envelope not yet audited (Pass 9).

### Pass 9 — DFM / solderability (adversarial, fresh eyes)

| # | Sev | Finding | Verified? | Fix / decision | Re-proof |
| --- | --- | --- | --- | --- | --- |
| 9.1 | **HIGH** | **Package choice is a gate, not a preference.** The ADS1220 ships as **RVA (VQFN-16, leadless, with a thermal pad)** and **PW (TSSOP-16)**. The L2 envelope bans leadless outright — a VQFN board would be unbuildable by the audience. | yes (datasheet Package Information + Table 5-1) | **PW mandatory, RVA forbidden** — recorded as K2, as a `[D]` constraint in §1, in the BOM note, and as a proposed REQUIREMENTS-gate item so nobody "optimises" the footprint later. The PW variant additionally has **no thermal pad at all** (Table 5-1 shows "—"), so there is no hidden joint to inspect. **Their pin numbering differs completely**, which is now flagged for `[S]` (RK12). | K2, RK10, RK12, §1, §8 |
| 9.2 | MED | 0.65 mm 16-lead pitch is the hardest joint yet attempted in the curriculum — harder than L1.01's USB-C. | yes (package dimensions) | Accepted as **forced but favourable**: gull-wing leads are visible, reworkable and wick-clearable, unlike a QFN. RK10 carries the technique (flux + drag solder + wick, SMD-before-THT, 16-pin continuity/short check before power). Every other new part is SOT-23-5 or 0805 — no regression elsewhere. | RK10 |
| 9.3 | MED | The 1 MΩ 0.1 % line (`RT0805BRD071ML`) has the thinnest stock on the board at **1,073**. | yes (live DigiKey) | Reduced the exposure instead of accepting it: the **two bias resistors were moved to the 1 % `RC0805FR-071ML`** (130,175 in stock) because they set a bias path, not a ratio — leaving **one piece per board** on the thin line. Alternates named (Vishay `TNPW08051M00BEEA`, Panasonic `ERA-6AEB105V`). | §8 |
| 9.4 | MED | U3 is effectively **single-sourced** — there is no pin-compatible 24-bit alternative. | yes (TI Device Comparison Table, §4) | Stated plainly rather than glossed: the **ADS1120** is pin-compatible in the same PW package and keeps the board buildable at 16 bits. Risk accepted and recorded (4,107 in stock, Active). | §8 |
| 9.5 | OK | Package-by-package against the L2 envelope: TSSOP-16 + SOT-23-5 (leaded), 0805 passives, THT buttons/headers/test points. Nothing leadless, nothing under 0805. Assembly order and courtyard/pin-1 marking captured in the silkscreen rules. | yes | confirmed | — |

**Residual:** the RF/regulatory conditional not yet run (Pass 10).

### Pass 10 — **CONDITIONAL: RF / regulatory** (adversarial, fresh eyes) — fires on the board's nature

| # | Sev | Finding | Verified? | Fix / decision | Re-proof |
| --- | --- | --- | --- | --- | --- |
| 10.1 | **HIGH** | **This conditional fires, and no project flag would have told anyone.** The protocol lists "RF / regulatory" as firing on the board's *nature*; the schema has only `hasMainsNet`, `hasLiIon`, `hasThermalConcern`, `requiresStripboard`. This board puts a **+20 dBm 2.4 GHz radiator tens of millimetres from a 0.41 µV<sub>pp</sub> front end** — on every previous WROOM board the antenna keep-out was a *regulatory* item; here it is also a **measurement-integrity** item, and it is the most load-bearing conditional on the board. | yes (protocol §"Conditional audits" + schema read) | **Ran it as a full pass.** Two halves: *(a) regulatory* — the WROOM-1 is pre-certified, so no board-level radiator cert is needed **provided the keep-out is honoured on all four copper layers** (inherited L1.01 M1/R4 → RK13, `[L]`); *(b) immunity* — the mitigation stack is quantified at **≈ −75 dB** (K18) plus ≥60 mm diagonal separation (§1 M6) plus the plane. **No project flag needs changing** (the schema has none to change); instead the conditional is written into §7 as an explicit checklist row so it cannot be lost. | §1, §7, K18, K35, RK9, RK13 |
| 10.2 | **HIGH** | **The immunity half cannot be closed at `[D]`, and saying otherwise would be the dishonest move.** Near-field coupling into a specific trace geometry has no credible closed form, and after Pass 5.1 the available margin is −75 dB, not −117 dB. | yes | **Defined an acceptance test instead of claiming a proof** (K35): at gain 128 / 20 SPS with `MUX=1110b`, the peak-to-peak spread with **Wi-Fi TX forced on** must not exceed the radio-off spread by more than the 0.41 µV<sub>pp</sub> band. Logged as owed at `[L]`/BRINGUP. RK9 status is "mitigations captured → verify at [L]/BRINGUP", not "de-risked". | K35, RK9 |
| 10.3 | MED | Placement was specified as coordinates in the first draft, copying L1.01's style — but L1.01 only fixed coordinates at layout, and fixing them here would be a fake `[D]` closure. | yes | §1 M6 now states a **placement *rule*** (antenna over one short edge, analog island at the diagonally opposite corner, ≥60 mm centre-to-centre, J4 on the analog end edge, target outline ~50 × 90 mm 4-layer) and defers geometry to `[L]`, exactly as L1.01 did. | §1 M6 |
| 10.4 | OK | Other conditionals re-checked against the design rather than against the flags: **mains/HV** — none, USB-only; **Li-ion** — none, and the DAG gives this board no `SHARED_BLOCK` edge to L2.01's battery module; **deep thermal** — largest new dissipator 26 mW (Pass 8.5); **stripboard** — fabbed 4-layer PCB. **All four flags are correct as they stand.** | yes (DB + §5) | confirmed — **no flag change requested** | header, §7 |

**Residual:** pedagogy not yet attacked (Pass 11).

### Pass 11 — Learnability / pedagogy + altitude (adversarial, fresh eyes)

| # | Sev | Finding | Verified? | Fix / decision | Re-proof |
| --- | --- | --- | --- | --- | --- |
| 11.1 | **HIGH** | **The obvious teaching arc — "watch the bridge read microvolts" — is a lie the board would tell.** Pass 5.3 showed the bridge drifts **±82.5 µV/°C**; a learner watching it and expecting 0.41 µV<sub>pp</sub> would conclude the ADC is broken and learn the opposite of the lesson. | yes (K15 + skeptic re-check) | **Rewrote the arc around what is actually true.** (1) Short the inputs internally (`MUX=1110b`) and find the *real* floor — immune to the source. (2) Press SW3 and check a **known 824 µV step** — and discover the *standard* (±1.3 µV) is coarser than the resolution (0.41 µV). (3) Walk the reference AVDD → internal → ratiometric and watch the drift change. (4) Turn the radio on. (5) Read the layout you were handed and find out what it cost. **The bridge's drift became a teaching asset instead of a bug.** | §1, §2, §7, RK5 |
| 11.2 | **HIGH** | **"Low-noise voltage reference" was about to be taught as "buy a reference IC" — which would have made the board measurably worse.** Wiring an external reference requires REFP1/REFN1, which *are* AIN0 and AIN3, forcing the signal onto the split, worse-matched AIN1/AIN2 pair (Pass 2.2). And the arithmetic is against it: for a bridge, **ratiometric cancels excitation drift, reference drift and reference noise outright**; a cheap hand-solderable reference (REF3025 ≈ 50 ppm/°C, LM4040 ≈ 100 ppm/°C) is **worse than the ADS1220's own internal 30 ppm/°C max**; a good one (REF5025A, 3 ppm/°C) costs more than the ADC. | **yes (datasheet §5/§8.3.3 + pin map + re-derived)** | **Decision recorded as a decision, not an omission** (§4, boxed): no external reference IC. What is taught instead is the *discipline* — ratiometric referencing, **Kelvin sensing at the excitation** (K22), a **reference filter deliberately matched to the signal filter** (K21), a quiet analog rail, and the internal reference kept as the **measured** contrast. **Tiebreaker: the DAG.** This board de-risks ADS1292R/ADS1299, where front-end discipline transfers and a reference part number does not. Net effect: ~$9 removed *and* a better measurement. | §4, §7, K5, K21, K22 |
| 11.3 | MED | Altitude: the concept load (differential vs single-ended, common-mode windows, PGA bypass, ratiometric, Kelvin sensing, ΔΣ aliasing, ENOB vs noise-free bits, ESL, thermal EMF, ground return) is large even for L2. | yes | Added the concept-tiering block to §7: **must-land** = shorted-input floor · the 824 µV step and its coarser standard · differential+common-mode · ratiometric+Kelvin · ground/placement with K34's two numbers. **Demoted to teardown asides the guide does not test:** ESL at 2.4 GHz, ferrite-vs-resistor peaking, the ±3 % notch-band argument, Johnson-noise budgeting, thermal-EMF arithmetic. | §7 |
| 11.4 | MED | The board risked reading as "L1.05 again, but a better chip". | yes | Sharpened the through-line: L1.05 taught *resolution ≠ accuracy* with an ADC that **was** the bottleneck; L2.02 hands over an ADC that **is not**, and makes the learner find what is. Stated in the "Teaches" cell, §1 and §2, with a prerequisite note to confirm L1.05 ships first. | header, §1, §2, §7 |
| 11.5 | LOW | The board could be unusable without an accessory nobody has. | yes | Confirmed **fully demonstrable with nothing attached**: on-board bridge + SW3 step + the internal shorted-input and system-monitor modes. The load cell is named as an optional accessory (DigiKey `1528-4540-ND`) and deliberately kept **out of `bom.csv`** — it is not a board component and "4540" does not resolve cleanly under strict matching (it collides with an unrelated TDK part). | §8 |

**Residual:** internal consistency + pipeline conformance not yet swept (Pass 12).

### Pass 12 — Internal consistency + pipeline conformance (adversarial, fresh eyes) — **a claim falsified**

| # | Sev | Finding | Verified? | Fix / decision | Re-proof |
| --- | --- | --- | --- | --- | --- |
| 12.1 | **HIGH** | **A load-bearing sourcing claim in §8 was simply wrong.** The doc asserted "**`Texas Instruments` is a NEW manufacturer string** — no existing part uses it." A strict query of the live `Part` table shows the catalog **already carries** `Texas Instruments` / `SN74AHCT125DR` and `Texas Instruments` / `TLV61048DBVR`. The earlier probe had only pattern-matched a handful of MPNs and never asked the question directly. | **yes (Prisma read of all 53 catalog parts)** | Corrected §8: **zero new manufacturer strings**. Kept the C0G vendor choice (Samsung + KEMET over the cheaper Murata) since consistency is still worth more than $0.63, but the *stated reason* is now true. **Recorded rather than quietly fixed** — this is exactly the "delegated/assumed facts can be wrong" failure the protocol exists to catch, and it was caught by a machine check, not a re-read. | §8 |
| 12.2 | **HIGH** | Related: **`Yageo` / `RC0805FR-071ML` (R24/R25) was labelled NEW but is already in the catalog** (created for L2.01's load-share gate pulldown). The BOM therefore has **8** new lines, not 9. | yes (strict `findFirst` per line) | Corrected §4, §8 and the `bom.csv` note; the totals line now reads **26 lines / 64 placements, 18 matched, 8 new / 28 new placements**. | §4, §8, bom.csv |
| 12.3 | MED | The BOM had never been run through the **project's own** parser — only eyeballed. | yes | Ran `parseBomCsv` + a per-line strict `(manufacturer, mpn)` lookup: **26 rows, 0 parse errors, 64 placements, 0 refDes/quantity mismatches, 18 matched, 8 unmatched (= the 8 new parts)**. Quoted multi-refDes fields and the `Würth Elektronik` ü both round-trip correctly. | bom.csv |
| 12.4 | MED | The live `REQUIREMENTS_REVIEW` checklist on revision `v1` is **boilerplate copied from other boards**: `WS2812 level-shift strategy`, `Servo brownout mitigation`, `ADC1-only constraint`, `Auto-shutoff prevention` — **4 of 5 items are N/A here**; only `WROOM antenna keep-out zone confirmed` applies. (The same template artefact L1.05's run flagged.) | yes (DB) | Not a design defect — an **owner item at the REQUIREMENTS gate**. §7 now lists six proposed replacements, each of which this design already evidences (differential-only; reference strategy; AVDD/DVDD split + sequencing; solid unsplit plane; PW-not-VQFN; keep-out). | §7, header |
| 12.5 | MED | `Project.targetCost` is **null** in the live DB, yet §8 spoke of a cost target. | yes (DB) | §8 now states **$30 as a proposal**, with the build-up (L1.01 core ≈ $11 + $17.3 new, of which the ADS1220 is $9.26) and the note that the design *removed* ~$9 by rejecting the external reference. Owner sets the field. | §8 |
| 12.6 | LOW | **refDes `C4` does not exist** on this board — an unexplained gap that reads as an error. | yes (grep + L1.01/L1.05 BOMs) | It is inherited: the L1.01 core numbering skips C4, and L1.05 kept the gap. **Documented as deliberate** so shared refDes map 1:1 across the family rather than silently renumbering. | §4 |
| 12.7 | OK | Cross-section sweep: refDes, values and quantities reconcile across §2, §3, §4, §6 and `bom.csv`; the `K1…K37` / `RK1…RK14` / refDes namespaces are disjoint (no "risk RK7 vs resistor R7" collision); all 16 ADS1220 pins appear exactly once in the §6 pin table; `bomFrozenAt` null, revision at `REQUIREMENTS`, `publishedRevisionId` null, **`accessTier` PREMIUM untouched**, no parts created, no BOM lines written — the `[D]` gate is respected. | yes (DB + grep) | confirmed | — |

**Residual:** the Pass 7/8 design changes and every correction must be re-proven holistically (Pass 13).

### Pass 13 — Re-proof after the design changes (engineer)

| # | Sev | Finding | Verified? | Fix / decision | Re-proof |
| --- | --- | --- | --- | --- | --- |
| 13.1 | — | Re-ran math + physics on the **three design changes** (TVS removed; R24/R25 bias added; U4 EN moved to GPIO4). **TVS removal:** node capacitance drops by ~3 pF per input — f<sub>DIFF</sub> shifts by < 0.03 %, K17 unchanged; the ±5 mV leakage-offset term disappears entirely. **R24/R25:** 1 MΩ against a 500 Ω bridge node loads it by 5×10⁻⁴ and contributes 0.02 nV/√Hz — K13, K17 and K19 all unchanged; they sit on the *header* side of R22/R23 so no bias current flows through the series resistors. **EN on GPIO4:** no analog effect (a static signal), and it makes K32 a hardware guarantee. | yes (re-derived) | Confirmed all three changes are isolated from the measurement path. | K13, K17, K19, K23, K32, K36 |
| 13.2 | — | Re-verified the BOM after the changes: the TVS line is gone; R24/R25 land on an **existing catalog line**; SW3, J4, TP3/TP4 raised the reuse quantities of B3F-1000 (2→3), PRPC040SAAN-RC (2→3), 5010 (1→2) and 5011 (1→2). Re-ran the parser + strict lookup: **26 rows, 0 errors, 64 placements, 0 mismatches, 18 matched, 8 unmatched.** | yes (parser + Prisma) | Import-ready. | bom.csv |
| 13.3 | — | Whole-doc numeric re-sweep: 0.41 µV<sub>pp</sub> / 0.09 µV<sub>RMS</sub> · 3.07 nV LSB · ±22.66 mV · 0.825–2.678 V · 1.65 V · 7.58 kHz / 159 kHz / −30.6 dB · −75 dB · 31.5 nV / 95.4 nV / +6.0 % · ±82.5 µV/°C · −824.3 µV ± 1.3 µV · 13.3 mA / 25.9 mW / ΔT 5.0 °C · 1.1 µF (0.85 µF worst) · 39 V/ms vs 20 V/ms · 330/660/165/165 mV · 6.67 MHz / 4 MHz · 2.5 % vs 3 % · 40 nV vs 950 nV · 26 lines / 64 placements / 8 new — every figure reconciles across §1–§8 and `bom.csv`. Grepped for every retracted claim: "−117 dB" and "NEW manufacturer string" now appear **only inside their own retractions** (K18 and this log), and there is no ferrite, no TVS on an analog input, no external reference IC and no single-ended path anywhere in the design. | yes (grep + read-through) | clean | whole doc |
| 13.4 | — | Re-ran the earlier lenses the changes could have disturbed: **requirements** (F1–F9 still each map to a net, a K-row, a BOM line and a risk; F8 is new and testable), **part-truth** (no new part introduced; the removed TVS was the only deletion), **DFM** (R24/R25 are 0805, no envelope change), **pedagogy** (open-sensor detection became a teachable feature), **conditionals** (26 mW unchanged ⇒ thermal still false; RF unchanged). | yes | confirmed | §1, §4, §5, §7 |

**Residual:** confirm a full all-lens sweep yields nothing new (Pass 14).

### Pass 14 — DRY PASS — confirming all-lens sweep (adversarial framing, fresh eyes)

| # | Sev | Finding | Verified? | Fix / decision | Re-proof |
| --- | --- | --- | --- | --- | --- |
| 14.1 | — | Re-attacked the revised design across **every** lens in one sweep — requirements & traceability, topology/net integrity (all 16 pins), math, physics, part-truth, footprint intent, power integrity, FMEA, DFM, sourcing, layout-readiness capture, pedagogy, internal consistency, pipeline conformance, and the RF conditional. All Pass 2–13 fixes are present, mutually consistent, and load-bearing; every ADS1220 figure re-checks against SBAS501D; the BOM re-parses and re-matches; the three design changes remain isolated; the `[S]`/`[L]`/BRINGUP items are stated as **owed**, not closed. | **yes** | — | — |
| 14.2 | LOW | Residual non-blocking items, all already disclosed: the owner-side `REQUIREMENTS_REVIEW` boilerplate (12.4), the null `targetCost` (12.5), U3's single-source position (9.4), and R17's 1,073-piece stock line (9.3). None is a design defect. | yes | Accepted / flagged for the owner. | — |
| 14.3 | — | Honest statement of what is **not** proven at `[D]`, so the dry pass is not read as more than it is: **(a)** RF immunity — mitigations quantified, acceptance is the empirical Wi-Fi-on/off test (K35, RK9); **(b)** every layout-domain claim in K34/RK14 — the plane-vs-trace budget is a *design rule with numbers*, and only a routed board can honour it; **(c)** footprint↔pinout pad-by-pad and the final GPIO/register maps (RK12); **(d)** the fab-DRU DRC. These are phase-gated by the protocol, not skipped. | yes | Scheduled to `[S]` / `[L]` / BRINGUP. | header |

**Residual after this pass: NONE — DRY.** A full all-lens sweep of the revised design
yielded **zero new material (CRITICAL/HIGH/MED) findings**; only the disclosed owner-side
and later-phase items remain. **Design-stage part-ready.** The `[S]`, `[L]` and BRINGUP
audits remain explicitly owed at their stages before their own irreversible steps —
**no parts have been created, no BOM imported, and the revision has not been advanced.**
