# Treat binary assets as binary so core.autocrlf can never line-ending-mangle
# them on commit (which truncated a committed PDF before this file existed).
# Intentionally NO `* text=auto` — that would mass-renormalize the existing
# tree; this list only pins known-binary types.
*.pdf   binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.webp  binary
*.ico   binary
*.glb   binary
*.woff  binary
*.woff2 binary
*.zip   binary
*.mp4   binary
*.webm  binary

# Git hooks are shell scripts; they must stay LF or `sh` breaks on macOS/Linux.
# Scoped (not a blanket rule) to match the pin-only approach above.
.githooks/** text eol=lf
