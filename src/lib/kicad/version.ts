// The KiCad standard-library SNAPSHOT tag (Phase C). Identifies which install
// the search index, the R2 symbol-lib store, and the KicadSymbolDefCache were
// built from. Bump on a KiCad upgrade to re-key the R2 path and bust the cache.
// (Distinct from the symbol-lib EMIT format version in symbol-lib.ts.)
export const KICAD_LIB_VERSION = "10.0";
